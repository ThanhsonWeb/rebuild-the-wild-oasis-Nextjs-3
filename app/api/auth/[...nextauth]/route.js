export { GET, POST } from "@/app/_lib/auth";

// import { GET, POST } from "@/app/_lib/auth";

// export { GET, POST };

// "Hey, my GET and POST handlers are over there in auth.js."

// auth.js contains the authentication logic.
//  route.js exposes that logic to Next.js 
// because Next.js only looks at route.js, not at auth.js.